<cas:workflow xmlns:cas="http://oodt.jpl.nasa.gov/1.0/cas" name="IngestInPlace" id="urn:imagecat:IngestInPlaceWorkflow">
 <tasks>
   <!--
     Per chunk, and only per chunk. Jaccard, CLIP and fg/bg used to run
     here too; they are corpus-wide and now live in IndexCorpusWorkflow,
     gated on OcrSettled so they run once rather than once per chunk.
   -->
   <task id="urn:imagecat:IngestInPlaceTask"/>
 </tasks>
</cas:workflow>
