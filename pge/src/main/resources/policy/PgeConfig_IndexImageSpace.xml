<?xml version="1.0" encoding="UTF-8"?>

<!--
  After IngestInPlace, increment ImageSpace CLIP/FAISS.
  The script no-ops unless IMAGE_SPACE_HOME is set.
  Incremental encode of new Solr ids, then rewrite FAISS.
  XML comments must not contain double-hyphen.
-->
<pgeConfig>
  <exe dir="[JobDir]" shell="bash">
     <cmd>mkdir -p [JobOutputDir] [JobLogDir]</cmd>
     <cmd>bash [PGE_ROOT]/bin/index-imagespace/index-imagespace.sh</cmd>
  </exe>
  <output>
    <dir path="[JobOutputDir]" createBeforeExe="false">
    </dir>
  </output>
  <customMetadata>
    <metadata key="ProductionDateTime" val="[DATE.UTC]"/>
    <metadata key="DateMilis" val="[DATE_TO_MILLIS([ProductionDateTime],UTC_FORMAT,1970-01-01)]"/>
    <metadata key="JobDir" val="[OODT_HOME]/data/jobs/imagespace/[DateMilis]"/>
    <metadata key="JobOutputDir" val="[JobDir]/output"/>
    <metadata key="JobLogDir" val="[JobDir]/logs"/>
  </customMetadata>
</pgeConfig>
